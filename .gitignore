# Work overlay files (symlinked from dotfiles-work)
system/.*.work

# Runtime / generated
config/karabiner/automatic_backups
config/pgcli/
config/pip/pip.conf
install/Brewfile.lock.json

# Work-specific (gitignored even if placed here manually)
config/iterm2/AppSupport
config/iterm2/sockets
config/sublime-text/rbql_table_index_hex

